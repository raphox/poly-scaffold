import type { z } from "zod";

export type T__Form<T> = Omit<T, "id">;
export type T__FormErrors<T> = Partial<Record<keyof T__Form<T>, string>>;

export type T__ZodSchema<T> = {
  [k in keyof Partial<T__Form<T>>]: z.ZodTypeAny;
};
